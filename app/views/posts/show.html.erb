<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @post.content %>
</p>

<%if current_user && @post.in?(current_user.posts) %>
  <%= link_to 'Edit', edit_post_path(@post) %> |
    <% end %>
<%= link_to 'Back', posts_path %>
